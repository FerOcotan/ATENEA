<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/dialog_bkg"
    android:id="@+id/main"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="16dp"

    tools:context=".HomeFragment">

    <ImageView
        android:layout_width="70dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center|top"
        android:scaleType="centerCrop"
        android:src="@drawable/round_remove_24"/>


    <ImageView
        android:layout_width="70dp"
        android:layout_height="100dp"
        android:layout_gravity="center|top"

        android:backgroundTint="#FFFFFF"
        android:src="@drawable/baseline_contact_emergency_24" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="10dp"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginStart="12dp"
            android:layout_marginTop="5dp"
            android:padding="8dp"
            android:text="Scann Activity"
            android:textColor="#551F1E1E"
            android:textSize="25sp" />

        <ImageView

            android:id="@+id/cancel_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="150dp"
            android:layout_marginTop="8dp"
            android:src="@drawable/baseline_clear_24" />


    </LinearLayout>

    <!-- Contenedor para la cámara -->
    <FrameLayout
        android:layout_width="200dp"
        android:layout_height="250dp"
        android:layout_gravity="center"
        android:background="@android:color/darker_gray">

        <!-- Icono de cámara en el centro -->
        <ImageView
            android:id="@+id/camera_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:scaleType="centerInside"
            android:src="@drawable/baseline_camera_alt_24"
            android:contentDescription="Icono de cámara" />
    </FrameLayout>

    <Spinner
        android:id="@+id/class_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:spinnerMode="dropdown"
        android:textColor="#551F1E1E"
        android:textSize="20sp"
        android:entries="@array/class_options" />


    <!-- Botones de confirmación y cancelación -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="24dp">

        <!-- Botón de Cancelar -->
        <ImageView
            android:id="@+id/cancel_butto"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="40dp"
            android:src="@drawable/baseline_clear_24"
            android:contentDescription="Botón de cancelar" />

        <!-- Botón de Confirmar -->
        <ImageView
            android:id="@+id/confirm_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/baseline_check_24"
            android:contentDescription="Botón de confirmar" />
    </LinearLayout>


</LinearLayout>